# 📱 MyApp — PWA обёртка для Google Apps Script

Устанавливаемое приложение для ПК и телефона, работающее через сайт.

---

## 📁 Структура файлов

```
ваш-репозиторий/
├── .github/
│   └── workflows/
│       └── deploy.yml       ← автодеплой на GitHub Pages
├── pwa/
│   ├── index.html           ← главная страница
│   ├── manifest.json        ← настройки PWA
│   ├── sw.js                ← Service Worker (кэш, оффлайн)
│   └── generate_icons.py    ← генератор иконок
└── README.md
```

---

## 🚀 Пошаговое развёртывание на GitHub

### ШАГ 1 — Создать аккаунт GitHub
Если нет аккаунта → зарегистрируйтесь на **github.com**

---

### ШАГ 2 — Создать новый репозиторий
1. Нажмите **+** → **New repository**
2. Название: `myapp` (или любое)
3. Выберите **Public** ⚠️ (GitHub Pages работает бесплатно только для публичных)
4. Нажмите **Create repository**

---

### ШАГ 3 — Загрузить файлы

**Вариант А — через браузер (просто):**
1. На странице репозитория нажмите **Add file** → **Upload files**
2. Загрузите все файлы из папки `pwa/` в корень
3. Отдельно создайте папку `.github/workflows/` и загрузите `deploy.yml`

**Вариант Б — через Git (быстрее):**
```bash
git clone https://github.com/ВАШ_НИК/myapp.git
cd myapp
# скопируйте все файлы из папки pwa/ сюда
git add .
git commit -m "init: добавил PWA"
git push origin main
```

---

### ШАГ 4 — Включить GitHub Pages
1. Перейдите в репозиторий → **Settings** (шестерёнка)
2. Слева нажмите **Pages**
3. В разделе **Source** выберите: **GitHub Actions**
4. Сохраните

---

### ШАГ 5 — Запустить деплой
1. Перейдите в **Actions** (вкладка)
2. Слева выберите **Deploy to GitHub Pages**
3. Нажмите **Run workflow** → **Run workflow**
4. Подождите ~1-2 минуты (зелёная галочка = успех ✅)

---

### ШАГ 6 — Получить ссылку
После деплоя ваш сайт-приложение доступен по адресу:
```
https://ВАШ_НИК.github.io/myapp/
```
*(ник и название репозитория — ваши)*

---

## 📲 Установка на устройства

### На Android (Chrome):
1. Откройте ссылку в Chrome
2. Внизу появится баннер **"Установить приложение"** — нажмите
3. Или: меню ⋮ → **Добавить на главный экран**

### На iPhone/iPad (Safari):
1. Откройте ссылку в **Safari** (важно!)
2. Нажмите кнопку **Поделиться** (квадрат со стрелкой)
3. Выберите **На экран «Домой»**
4. Нажмите **Добавить**

### На ПК (Windows/Mac/Linux) через Chrome:
1. Откройте ссылку в Chrome/Edge
2. В адресной строке появится иконка установки 💻
3. Или: меню ⋮ → **Установить MyApp**
4. Приложение появится в списке программ!

---

## ✏️ Кастомизация

Отредактируйте в `index.html`:
- `<title>MyApp</title>` — название в браузере
- `.header-brand` текст — название в шапке
- `.splash-title` текст — название при загрузке
- Цвета в `:root` — цветовая схема

Отредактируйте в `manifest.json`:
- `"name"` — полное имя приложения
- `"short_name"` — короткое имя (на иконке)
- `"description"` — описание

---

## 🔒 Скрытие URL

URL вашего Google Apps Script **НЕ виден** пользователям:
- Он спрятан внутри кода
- Пользователи видят только ваш GitHub Pages адрес
- Для дополнительной защиты можно сделать репозиторий **Private**
  (тогда нужен GitHub Pro или настройка через GitHub Actions)

---

## ❓ Часто задаваемые вопросы

**Нужен ли хостинг?** — Нет! GitHub Pages бесплатный.

**Будет ли работать без интернета?** — Оболочка загрузится, но сам сервис Google требует интернет.

**Как обновить?** — Измените файлы и сделайте push. Деплой запустится автоматически.

**Иконки не генерируются?** — Создайте папку `icons/` и добавьте любые PNG 192x192 и 512x512 вручную.
